<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ScreenCapture.Views.EditorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ScreenCapture.Views"
    xmlns:skia="using:SkiaSharp.Views.Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="ScreenCapture - Editor">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="8" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="4" Grid.Row="0">
            <ToggleButton x:Name="RectangleToolButton" Content="Chữ nhật" Click="RectangleToolButton_Click" />
            <ToggleButton x:Name="ArrowToolButton" Content="Mũi tên" Click="ArrowToolButton_Click" />
            <ToggleButton x:Name="TextToolButton" Content="Text" Click="TextToolButton_Click" />
            <ToggleButton x:Name="CropToolButton" Content="Cắt" Click="CropToolButton_Click" />
            <AppBarSeparator />
            <Button x:Name="UndoButton" Content="Undo" Click="UndoButton_Click" />
            <Button x:Name="RedoButton" Content="Redo" Click="RedoButton_Click" />
            <AppBarSeparator />
            <Button Content="Lưu PNG" Click="SaveButton_Click" />
            <Button Content="Copy" Click="CopyButton_Click" />
        </StackPanel>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                      HorizontalContentAlignment="Left" VerticalContentAlignment="Top">
            <Border BorderBrush="Gray" BorderThickness="1" HorizontalAlignment="Left" VerticalAlignment="Top">
                <skia:SKXamlCanvas x:Name="Canvas"
                                    PaintSurface="Canvas_PaintSurface"
                                    PointerPressed="Canvas_PointerPressed"
                                    PointerMoved="Canvas_PointerMoved"
                                    PointerReleased="Canvas_PointerReleased" />
            </Border>
        </ScrollViewer>

        <TextBlock x:Name="StatusText" Grid.Row="2" Opacity="0.7" />
    </Grid>
</Window>
