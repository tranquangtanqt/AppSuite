<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Mcf.Screen.HtmlGenerator.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mcf.Screen.HtmlGenerator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Module G">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="2">
            <TextBlock Text="Module G - Tai lieu man hinh (画面説明書)" Style="{StaticResource TitleLargeTextBlockStyle}" />
            <TextBlock Text="Doc toan bo *.xlsx trong thu muc nguon -&gt; luu Data\Database\01_画面説明書.db -&gt; xuat Data\Database\Html\01_画面説明書.html + 1 file/man hinh" Opacity="0.7" TextWrapping="Wrap" />
        </StackPanel>

        <Grid Grid.Row="1" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{x:Bind ViewModel.SourceFolder, Mode=OneWay}" IsReadOnly="True"
                     PlaceholderText="Chua chon thu muc nguon (chua *.xlsx 画面説明書)" />
            <Button Grid.Column="1" Content="Chon thu muc nguon..." Click="ChooseFolderButton_Click" />
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8">
            <Button Content="1. Doc Excel -> SQLite" Command="{x:Bind ViewModel.ImportCommand}" Style="{StaticResource AccentButtonStyle}" />
            <Button Content="2. Xuat HTML" Command="{x:Bind ViewModel.ExportHtmlCommand}" />
            <Button Content="Mo file HTML" Command="{x:Bind ViewModel.OpenHtmlCommand}" />
        </StackPanel>

        <ProgressBar Grid.Row="3" IsIndeterminate="{x:Bind ViewModel.IsBusy, Mode=OneWay}" />

        <TextBlock Grid.Row="4" Text="{x:Bind ViewModel.StatusText, Mode=OneWay}" TextWrapping="Wrap" />

        <Border Grid.Row="5" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="4">
            <ListView ItemsSource="{x:Bind ViewModel.LogLines}" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="x:String">
                        <TextBlock Text="{x:Bind}" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Window>
