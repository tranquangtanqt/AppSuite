<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ModuleD.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ModuleD"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Module D">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <ScrollViewer HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel Padding="32" Spacing="12" Width="360">
            <TextBlock Text="Module D" Style="{StaticResource TitleLargeTextBlockStyle}" HorizontalAlignment="Center" />
            <TextBlock Text="Form nhap lieu" Opacity="0.7" HorizontalAlignment="Center" Margin="0,0,0,12" />

            <TextBox x:Name="NameBox" Header="Ho ten" PlaceholderText="Nhap ho ten" />
            <TextBox x:Name="EmailBox" Header="Email" PlaceholderText="Nhap email" />
            <TextBox x:Name="NoteBox" Header="Ghi chu" PlaceholderText="Nhap ghi chu" AcceptsReturn="True" TextWrapping="Wrap" Height="80" />

            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right" Margin="0,8,0,0">
                <Button x:Name="ClearButton" Content="Xoa" Click="ClearButton_Click" />
                <Button x:Name="SubmitButton" Content="Luu" Style="{StaticResource AccentButtonStyle}" Click="SubmitButton_Click" />
            </StackPanel>

            <InfoBar x:Name="ResultInfoBar" IsOpen="False" />
        </StackPanel>
    </ScrollViewer>
</Window>
