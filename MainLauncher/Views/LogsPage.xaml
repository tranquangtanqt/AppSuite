<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MainLauncher.Views.LogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Common.Models"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Logs" Style="{StaticResource TitleLargeTextBlockStyle}" Grid.Row="0" Margin="0,0,0,16" />

        <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.Entries}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:LogEntry">
                    <TextBlock Text="{x:Bind DisplayText}" FontFamily="Cascadia Mono" FontSize="12" TextWrapping="Wrap" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
