<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ScreenCapture.Views.RegionOverlayWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ScreenCapture.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="RootGrid">
        <Image x:Name="BackgroundImage" Stretch="Fill" HorizontalAlignment="Left" VerticalAlignment="Top" />
        <Canvas x:Name="OverlayCanvas"
                Background="Transparent"
                PointerPressed="OverlayCanvas_PointerPressed"
                PointerMoved="OverlayCanvas_PointerMoved"
                PointerReleased="OverlayCanvas_PointerReleased" />
        <Border HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,24,0,0"
                Padding="12,6" CornerRadius="4" IsHitTestVisible="False">
            <Border.Background>
                <SolidColorBrush Color="Black" Opacity="0.6" />
            </Border.Background>
            <TextBlock x:Name="HintText" Text="Kéo chuột để chọn vùng" Foreground="White" FontSize="16" />
        </Border>
    </Grid>
</Window>
